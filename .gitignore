# Archives
*.zip

# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python

# Virtual environments
venv/
venv_macos/
.venv/
env/

# ML model binaries (large)
codebase/Ml_MP/TruthLens/models/*.joblib
codebase/Ml_MP/TruthLens/models/*.pkl

# Processed data — isot_processed.csv is 317MB, exceeds GitHub's 100MB limit
codebase/Ml_MP/TruthLens/data/processed/isot_processed.csv

# Old codebase backup
codebase/Ml_MP_old/

# Presentations — gitignore non-submission PDFs
presentations/ML_Imp-Ph-1presentation.pdf
presentations/TruthLens_*.pdf
presentations/TruthLens_*
presentations/TruthLens *

# Stray PDFs inside codebase
codebase/Ml_MP/*.pdf
codebase/Ml_MP/*.pptx

# RAG cache
codebase/Ml_MP/Truth/backend/rag_cache.json

# Local secrets (use .env.example as a template)
.env
codebase/Ml_MP/Truth/backend/.env

# Local Claude Code workspace state (project-local config and plan files)
.claude/

# Dataset binaries — too large for GitHub, ship pointers instead
datasets/ISOT/
datasets/LIAR/*.csv
datasets/LIAR/*.tsv
codebase/Ml_MP/TruthLens/data/processed/*.csv
codebase/Ml_MP/TruthLens/data/raw/

# Backend test fixtures (regenerated by canonical-input runs)
codebase/Ml_MP/Truth/backend/tests/fixtures/
codebase/Ml_MP/Truth/backend/tests/__pycache__/

# Stray TruthLens PDF exports at repo root
TruthLens_*.pdf

# OS
.DS_Store
Thumbs.db


ui.mp4

# Local-only / college-process artifacts (not for the public repo)
presentations/
research/
imp code and algo/
workflow_diagram.py
datasets/LIAR/README